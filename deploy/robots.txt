User-agent: *
Allow: /

# The content studio is a private Netlify Identity area with nothing to index.
Disallow: /admin/

# Generator input: the Markdown articles and the multi-language home page
# template. Every word of it is already served, in a better form, from the
# pages built out of it. Nothing links here.
Disallow: /content/

# Build machinery. The publish directory is the project root, so the generators,
# the Tailwind config, the Tailwind entry file, the build manifest and the
# lockfile all sat alongside the pages and were served like any other file. Most
# of them now answer 404 - see the block at the bottom of _redirects - but a
# crawler that has one of these addresses from before should be told not to ask
# again rather than be sent to a 404 to find out.
#
# This is a crawl directive, not access control: it asks well-behaved crawlers
# not to fetch these paths. The matching X-Robots-Tag headers in _headers are
# what keep them out of an index if something fetches them anyway.
Disallow: /scripts/
Disallow: /node_modules/
Disallow: /tailwind.config.js
Disallow: /assets/tailwind.css
Disallow: /package.json
Disallow: /package-lock.json

# The assistants that answer questions from the open web are the second audience
# for this site, after search. They are welcome, and llms.txt is written for
# them, but each one reads robots.txt under its own product name and several
# treat the absence of their name as a reason to be conservative. Naming them
# removes the ambiguity. Note that a user-agent group replaces the "*" group
# rather than extending it, so each one restates the disallow list.
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: Claude-User
User-agent: Claude-SearchBot
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: Google-Extended
User-agent: Applebot-Extended
User-agent: CCBot
Allow: /
Disallow: /admin/
Disallow: /content/
Disallow: /scripts/
Disallow: /node_modules/
Disallow: /tailwind.config.js
Disallow: /assets/tailwind.css
Disallow: /package.json
Disallow: /package-lock.json

Sitemap: https://compoundingjourney.com/sitemap.xml

# There is no registered robots.txt directive for llms.txt, so these are
# comments rather than fields - a crawler that does not know them ignores them,
# which is the point. They are here because robots.txt is the one file every
# crawler fetches first, and until now nothing on the site said these existed.
# llms.txt: https://compoundingjourney.com/llms.txt
# llms-full.txt: https://compoundingjourney.com/llms-full.txt
